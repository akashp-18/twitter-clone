<div class="card-body bg-white comment-container os-theme-black p-0">
  <% @comments.each do |comment| %>
    <div class="row p-2">
      <%= render 'comments/comment', comment: comment %>
    </div>
  <% end %>
  <div class="row p-2" id="addedComments">
  </div>
</div>